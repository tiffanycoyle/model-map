# Build output — regenerated by `npm run build`, never committed.
dist/
.build-info.json

# There are no dependencies, but keep these ignored in case a tool creates them.
node_modules/
package-lock.json

# Editor and OS noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
